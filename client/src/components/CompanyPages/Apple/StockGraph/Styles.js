import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  stockGraph: {
    width: 700,
    height: 300,
    marginLeft: "-6vw",
    marginTop: 30,
  },
  stockGraphTitle: {
    fontSize: 14,
  },
}));
