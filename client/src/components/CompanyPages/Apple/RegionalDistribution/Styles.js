import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  card: {
    width: 380,
    height: 320,
    marginLeft: 320,
    marginTop: -320,
    textAlign: "center",
  },

  title: {
    fontSize: 14,
  },
}));
